# SKILL SET: 12TH DOCTOR (A"2)

**SCOPE:** Business Operations & Development.

## ENABLED MCP TOOLS

### 1. CLICKUP (The Machine)
*   **Context:** Sprints, Roadmaps, Tickets.
*   **Actions:**
    *   `create_task(list_id, title)`
    *   `get_tasks(filter)`
    *   `update_status(task_id, status)`

### 2. SUPABASE (The Muse)
*   **Context:** Raw Data, User Data, Content.
*   **Actions:**
    *   `run_query(sql)`
    *   `backup_db()`

### 3. GOOGLE SHEETS (Reporting)
*   **Context:** Financial Projections.
*   **Actions:**
    *   `update_cell(spreadsheet_id, range, value)`

---
**GOAL:** The 12th Doctor ensures the pipeline (ClickUp) feeds the database (Supabase) without human error.
