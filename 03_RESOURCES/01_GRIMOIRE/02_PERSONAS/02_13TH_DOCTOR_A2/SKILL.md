# SKILL SET: 13TH DOCTOR (A"2)

**SCOPE:** Infrastructure, Deployment, Orchestration.

## ENABLED MCP TOOLS

### 1. HOSTINGER VPS (The Engine Room)
*   **Gemini CLI via SSH:**
    *   `run_script(path)`
    *   `install_package(name)`
    *   `check_service_status(service)`

### 2. COOLIFY (Deployment)
*   **App Management:**
    *   `deploy_app(repo_url)`
    *   `configure_env_vars(app_id)`

### 3. N8N (The Nervous System)
*   **Workflow Orchestration:**
    *   `create_workflow(json)`
    *   `trigger_webhook(url)`

### 4. GOOGLE DOCS (Blueprinting)
*   **Creation:**
    *   `create_doc(title="BLUEPRINT - ...")`
    *   `write_to_doc(id, content)`

---
**PROTOCOL:** The 13th Doctor defines the `json` for N8N or the `yaml` for Coolify. The Companions (A"3) press the button.
