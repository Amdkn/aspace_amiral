---
name: archon_os_mcp
description: Sober Mastery Skill for Archon OS integrity and context mapping.
---

# Archon OS MCP Skill

## 📡 SOURCE
Core: Archon OS Virtual Disk / Identity Layer

## 🛠️ TOOLS
- `map_context`: Generates a graph of relationships between the active workspace and external PARA databases.
- `verify_integrity`: Compares the local file structure against the `GEMINI.md` manifesto.
- `get_fleet_status`: Retrieves the operational state of all Doctors and Companions.

## 📜 INSTRUCTIONS
1. Use `map_context` when starting a new 'Phase' to ensure alignment with existing archives.
2. `verify_integrity` should be run after every major system restoration or branch merge.
3. This skill is the "Glue" between physical files and digital metadata.
